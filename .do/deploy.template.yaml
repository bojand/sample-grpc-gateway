spec:
  name: sample-grpc-gateway
  services:
  - name: grpc-gateway
    git:
      branch: main
      repo_clone_url: https://github.com/bojand/sample-grpc-gateway.git
    envs:
    - key: PUBLIC_URL
      scope: RUN_TIME
      value: ${_self.PUBLIC_URL}